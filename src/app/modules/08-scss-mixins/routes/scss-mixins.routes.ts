import { Routes } from '@angular/router';
import { ScssMixinsComponent } from '../containers/scss-mixins/scss-mixins.component';

export const routes: Routes = [
  {
    path: '',
    component: ScssMixinsComponent
  }
]
