import { Routes } from '@angular/router';
import { ScssPartialsComponent } from '../containers/scss-partials/scss-partials.component';

export const routes: Routes = [
  {
    path: '',
    component: ScssPartialsComponent
  }
]
