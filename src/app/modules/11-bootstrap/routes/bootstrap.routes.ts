import { Routes } from '@angular/router';
import { BootstrapComponent } from '../containers/bootstrap/bootstrap.component';

export const routes: Routes = [
  {
    path: '',
    component: BootstrapComponent
  }
]
